<%- include('partials/header') %>

<main class="container py-4">

<!-- Welcome -->
<div class="text-center">
    <h2 class="h1 my-4">Bienvenue sur O'Quiz</h2>
    <p class="lead">Jus de réglisse écraseur profiteur patagon. Phylloxera boit-sans-soif brontosaure
        coléoptère, rhizopode amiral de bateau-lavoir ivrogne schizophrène traître. Autodidacte mussolini de
        carnaval Ku-Klux-Klan trompe-la-mort, dynamiteur invertébré jocrisse vercingétorix de carnaval corsaire.
        Gibier de potence rapace amiral de bateau-lavoir. Projectile guidé brute, sauvage gangster aérolithe.
        Gyroscope trafiquant de chair humaine saltimbanque, mercenaire olibrius, malotru cow-boy de la route
        rapace moricaud équilibriste.</p>
</div>

<!-- Cards -->
<div class="row mb-3 text-center flew-wrap">

    <% quizz.forEach(quiz => { %>
        <!-- One card -->
        <div class="col-4">
            <div class="card mb-4">
                <div class="card-header">
                    <h3 class="h2"><%= quiz.title %></h3>
                </div>
                <div class="card-body d-flex flex-column justify-content-between">
                    <h4 class="h3"><%= quiz.description %></h4>

                    <p>par <%= quiz.author.firstname %> <%= quiz.author.lastname %></p>
                    <ul class="list-unstyled mb-4 p-0">
                        <% quiz.tags.forEach(tag => { %>
                        <li class="badge bg-primary"><%= tag.name %></li>
                        <% }) %>

                    </ul>
                    <a href="/quiz/<%= quiz.id %>" class="btn btn-lg btn-primary text-white">Jouer</a>
                </div>
            </div>
        </div>
    <% }) %>
</div>
</main>

<%- include('partials/footer') %>
